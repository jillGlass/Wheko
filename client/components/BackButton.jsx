import React from "react";
import { Button } from "react-bootstrap";

const BackButton = () => <Button style={{ margin: '10px' }}size="lg" variant="outline-success">Back</Button>;

export default BackButton;
